import 'package:flutter/material.dart';

class WeaverColors {
  WeaverColors._();

  // Backgrounds
  static const background = Color(0xFF0A0B0F);
  static const surface = Color(0xFF12141A);
  static const card = Color(0xFF1A1D26);
  static const cardHover = Color(0xFF1F2330);
  static const cardBorder = Color(0xFF2A2F3D);

  // Accent — silk/amber gold
  static const accent = Color(0xFFC8973A);
  static const accentDim = Color(0xFF8A6520);
  static const accentGlow = Color(0x33C8973A);
  static const accentBright = Color(0xFFE8B445);

  // Semantic
  static const success = Color(0xFF3ABFA8);
  static const successDim = Color(0xFF1E6B5E);
  static const error = Color(0xFFE05252);
  static const errorDim = Color(0xFF7A2929);
  static const warning = Color(0xFFE09520);
  static const warningDim = Color(0xFF7A5210);
  static const info = Color(0xFF5B8DEF);
  static const infoDim = Color(0xFF2A4A8A);

  // Text
  static const textPrimary = Color(0xFFEEF0F7);
  static const textSecondary = Color(0xFFB0B8CC);
  static const textMuted = Color(0xFF6B7280);
  static const textDisabled = Color(0xFF3A3F50);

  // Tool category colors
  static const cloudColor = Color(0xFF5B8DEF);
  static const messagingColor = Color(0xFF7B61FF);
  static const filesColor = Color(0xFF3ABFA8);
  static const devColor = Color(0xFFE09520);
  static const productivityColor = Color(0xFFE05252);

  // Node colors for workflow
  static const triggerNode = Color(0xFF7B61FF);
  static const actionNode = Color(0xFF3ABFA8);
  static const conditionNode = Color(0xFFE09520);
  static const outputNode = Color(0xFFC8973A);
}
